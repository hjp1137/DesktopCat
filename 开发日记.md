# 开发日记

## 2026-09-02
- 初始化 DesktopCat 项目仓库与 Git 版本控制，添加 Godot 4 忽略规则。
- 创建 Godot 4.7.2 基础工程配置 `project.godot` 与主场景 `scenes/main.tscn`。
- 在 `scripts/cat.gd` 中实现基于原生 `_draw()` 绘制的临时矢量猫形占位对象，无需外部图片资源。
- 实现小猫基于 delta 的平滑移动逻辑与视口左右边界碰撞反弹、水平朝向翻转。
- 编写自动化单元测试与集成测试脚本，通过 Godot 4.7.2 Console 验证无语法与运行时错误，且完成双向边界碰撞转向闭环测试。
- 完成 T02 透明桌面 Overlay：
  - 在 `project.godot` 与 `main.gd` 中开启逐像素透明（per_pixel_transparency）、视口透明背景（transparent_background）、无边框（borderless）与窗口置顶（always_on_top）；
  - 针对 Windows DWM 全屏独占导致黑屏问题，在 `main.gd` 中通过 `screen_get_usable_rect` 避免全屏独占优化，显式调用 `DisplayServer.window_set_flag` 确保系统级透明生效；
  - 适配多显示器扩展屏场景（当前设备包含屏幕0: 2880x1920，屏幕1: 5760x3240），支持按 `TAB` 键在多显示器之间动态切换 Overlay 所在屏幕；
  - 适配小猫在全屏显示区域内的左右巡逻与边界反弹；
  - 增加 ESC 键与 Alt+F4 安全退出逻辑；
  - 生成独立单文件 EXE 构建产物，并通过本地真实 GUI 测试验证。
- 完成 T03 鼠标穿透与小猫点击互动：
  - 调用 `DisplayServer.window_set_mouse_passthrough` 实现动态鼠标穿透，仅小猫所在包围盒区域响应输入，其余全屏透明区域完全穿透到 Windows 桌面及底层应用；
  - 穿透多边形实时跟随小猫移动动态同步更新，无鼠标残留阻挡；
  - 实现小猫点击交互：点击小猫输出 `Cat clicked!` 并触发向上 16 像素平滑弹跳反馈，不中断自主巡逻与碰壁反弹；
  - 更新单元与集成测试，并重新构建单文件独立 EXE。
- 完成 T04 正式 Cat 表现层与 Idle/Walk 动画框架：
  - 生成 64x64 原创临时动画素材（`idle` 4 帧 @ 4 FPS，`walk` 6 帧 @ 10 FPS）；
  - 将小猫独立为 `scenes/cat.tscn`，配置 `AnimatedSprite2D` 与 `Area2D`；
  - 在 `cat.gd` 中实现基础走走停停状态机（WALK 3~7s ↔ IDLE 1.5~3.5s），状态切换时驱动对应动画播放；
  - 行走朝向由 `AnimatedSprite2D.flip_h` 精确翻转；
  - 保持点击弹跳、鼠标穿透、多屏切换与独立 EXE 运行全部正常。
- 完成 T05 行为状态机 + 统一 Command 系统：
  - 建立 `scripts/command_manager.gd` 独立指令中枢，定义 `CatCommand` 标准指令枚举；
  - 将小猫状态机重构为标准生命周期模型（`enter_state`、`update_state`、`exit_state`、`change_state`）；
  - 实现 `AUTO`（自主走走停停）与 `COMMAND`（服从指令）两种模式，外部命令可即时打断自主行为；
  - 在 `main.gd` 中接入键盘数字键 1/2/3/4 测试指令分发；
  - 重新构建单文件独立 EXE 并完成全链路自验证。
- 完成 T06 Jump + Gravity + Falling 垂直物理系统：
  - 构建完整的跳跃物理（JUMP → FALL → LAND），动态计算 `ground_y`，落地位置稳定无穿地；
  - 扩展 `CatState`（`IDLE`, `WALK`, `JUMP`, `FALL`）与 `CatCommand`（`JUMP`）；
  - 落地后根据当前模式（AUTO / COMMAND）自动恢复原意图地面行为；
  - 实现空中防二段跳与空中保持水平位移；
  - 彻底分离物理 Jump 与点击 16px 视觉反馈，空中鼠标穿透实时跟随 Cat 移动；
  - 在 `main.gd` 中接入键盘数字键 5 发送 JUMP 命令，并重新构建单文件独立 EXE。
- 完成 T07 Mouse Drag + Throw 鼠标拖拽与抛掷交互系统：
  - 新建 `scripts/mouse_controller.gd` 独立输入控制器，8 像素阈值区分 Click 与 Drag；
  - 扩展 `CatCommand`（`DRAG_START`, `DRAG_MOVE`, `DRAG_END`）与 `CatState`（`DRAG`）；
  - 拖拽过程保持相对抓取偏移（`drag_offset`），暂停重力与计时器；
  - 采样最近 120ms 样本平滑计算 Throw 速度并做安全限制（X: 1200, Y: 1000）；
  - 释放后完全复用 T06 重力、抛物线与落地系统，撞墙衰减反弹，顶部安全限制；
  - 拖拽期间全屏接收鼠标并具备物理释放 Fail-safe 保护，释放后立即恢复 Mouse Passthrough；
  - 拖拽期间采取外部指令忽略策略防止被打断，落地后恢复原模式地面行为。
- 完成 T08 生活行为系统 (RUN + SIT + SLEEP + Autonomous Behavior Scheduler)：
  - 扩展 `CatState`（`RUN`, `SIT`, `SLEEP`）与 `CatCommand`（`RUN_LEFT`, `RUN_RIGHT`, `SIT`, `SLEEP`, `WAKE`）；
  - 生成 `run` (16 FPS)、`sit` (2 FPS)、`sleep` (3 FPS) 占位动画并在 `SpriteFrames` 中配置；
  - 实现冲刺奔跑（`run_speed = 220.0` px/s）、端坐静止与睡眠呼吸（8~16s 自动醒来或 WAKE 唤醒）；
  - 睡眠时收到 JUMP 自动唤醒起跳，被 Drag 抓住立即进入拖拽且落地转为 IDLE 并重置 15s 睡眠冷却；
  - 实现基于转移权重池（WALK 40%, SIT 25%, IDLE 15%, RUN 15%, SLEEP 5%）、持续范围与冷却机制（`sleep_cooldown` 15s, `run_cooldown` 6s）的自主生活行为调度器，具备边缘感知转向；
  - 在 `main.gd` 中接入键盘快捷键 6/7/8/9/0，更新单元/集成测试并重新构建单文件独立 EXE。
