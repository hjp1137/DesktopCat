# 开发日记

## 2026-09-02
- 初始化 DesktopCat 项目仓库与 Git 版本控制，添加 Godot 4 忽略规则。
- 创建 Godot 4.7.2 基础工程配置 `project.godot` 与主场景 `scenes/main.tscn`。
- 在 `scripts/cat.gd` 中实现基于原生 `_draw()` 绘制的临时矢量猫形占位对象，无需外部图片资源。
- 实现小猫基于 delta 的平滑移动逻辑与视口左右边界碰撞反弹、水平朝向翻转。
- 编写自动化单元测试与集成测试脚本，通过 Godot 4.7.2 Console 验证无语法与运行时错误，且完成双向边界碰撞转向闭环测试。
- 完成 T02 透明桌面 Overlay：开启逐像素透明、透明背景、无边框、置顶与多屏切换。
- 完成 T03 鼠标穿透与小猫点击互动：动态 Passthrough 跟随与 16px 视觉反馈。
- 完成 T04 正式 Cat 表现层与 Idle/Walk 动画框架：生成动画帧并支持自主走走停停。
- 完成 T05 行为状态机 + 统一 Command 系统：建立 CommandManager 与 AUTO/COMMAND 模式。
- 完成 T06 Jump + Gravity + Falling 垂直物理系统：跳跃下落落地与空中水平位移。
- 完成 T07 Mouse Drag + Throw 拖拽与抛掷交互系统：平滑速度采样、边界反弹与释放保护。
- 完成 T08 生活行为系统：RUN、SIT、SLEEP 状态与基于权重/冷却的自主调度器。

## 2026-09-03
- 完成 T09 指针感知、注意力与目标移动系统 (Pointer Perception + Attention + Target Movement)：
  - 新建 `scripts/mouse_perception_controller.gd`，利用 `DisplayServer.mouse_get_position()` 读取全局鼠标坐标并动态换算为当前 Overlay 局部坐标；
  - 扩展 `CatCommand` 通用目标指令：`LOOK_AT_POSITION`、`MOVE_TO_POSITION`、`CLEAR_TARGET`，小猫仅依赖通用目标坐标，为未来手势与 AI 保持输入源无关；
  - 实现被动注视（Attention）：`attention_radius = 400.0` px，`attention_exit_radius = 460.0` px 滞后区间，在 IDLE/SIT 状态下带 20px 死区平滑转头；
  - 实现好奇跟随 (Curiosity Chase)：AUTO 模式下鼠标在 350px 内快速晃动时触发 2.5~4.5 秒追逐，结束后进入 15 秒冷却；
  - 实现目标移动跟随：保持 `pointer_follow_distance = 64.0` px 偏置与 `target_reached_radius = 50.0` px 达到死区，不压住鼠标；
  - 显式用户控制与 DRAG 状态拥有最高优先级，立即打断目标跟随；
  - 在 `main.gd` 中接入快捷键 `C` 切换 Debug Follow 模式，更新测试用例并重新构建单文件独立 EXE。
- 完成 T10 本地外部感知桥接通道 (Local External Perception Bridge)：
  - 新建 `scripts/external_bridge.gd`，利用 Godot 原生 `TCPServer` 仅监听 `127.0.0.1:47831`，非阻塞 poll 处理客户端连接与数据接收；
  - 制定并编写 `docs/external_bridge_protocol.md`，基于 TCP + UTF-8 + NDJSON (v1) 规范定义消息信封；
  - 设置 256KB 单消息上限与 512KB 缓冲区上限，实现畸形 JSON、未知指令与异常坐标防御性拦截；
  - 外部指令严格经白名单校验后下发至统一 `CommandManager`，与本地键盘、鼠标感知指令完全同构；
  - 客户端连接断开与平滑重连逻辑独立，编写 Python 标准库调试客户端 `tools/bridge_test_client.py` 并通过 20Hz 压力测试。
- 完成 T11 Windows 顶层窗口几何感知系统 (Window Geometry Perception)：
  - 新建正式感知服务 `tools/perception/window_perception.py`，仅依赖 Python 标准库与 `ctypes`（`user32.dll` + `dwmapi.dll`），零第三方包；
  - 枚举顶层窗口并通过 `DwmGetWindowAttribute` 获取精确扩展包围盒，过滤最小化、不可见、尺寸 <= 32px 的微型辅助窗口、系统任务栏与壁纸；
  - 过滤 DesktopCat 自身 Overlay 与感知服务控制台，通过 `MonitorFromWindow` 换算当前屏幕工作区物理坐标与高 DPI 缩放比例，执行屏幕边界裁剪（Rect Clipping）；
  - 实现 5Hz 低频扫描与基于几何签名的变动检测（Change Detection），仅在窗口变动时发送 `window_snapshot`，静止时 CPU 占用近 0%，零网络冗余流量；
  - 新建 `scripts/world/window_world_model.gd` 纯几何数据模型，不生成物理碰撞体，保持小猫现有世界活动与 ground_y 零干扰；
  - 接入 `F8` 快捷键切换线框调试显示，纯视觉线框不影响底层窗口正常鼠标点击与穿透交互；
  - 更新单元测试、集成测试与构建脚本，重新打包单文件独立版 `DesktopCat_Standalone.exe` 并通过双向测试。
- 完成 T12 窗口几何转 Surface 物理表面世界 (Window Geometry → Surface World)：
  - 新建 `scripts/world/surface.gd`，支持 `PLATFORM` 与 `WALL` 类型，定义 `TOP`、`BOTTOM`、`LEFT`、`RIGHT` 方向与 `walkable`、`dynamic` 物理属性；
  - 建立稳定 ID 体系（`<wid>:top`, `<wid>:bottom`, `<wid>:left`, `<wid>:right` 以及 `screen:ground`, `screen:left`, `screen:right`, `screen:top`）；
  - 新建 `scripts/world/surface_world_model.gd`，实现轻量顶边遮挡切分（Top Platform Occlusion）与全遮挡剔除，自动处理上层窗口覆盖与短片段过滤；
  - 设置 `MIN_PLATFORM_LENGTH = 48.0` px，`MIN_WALL_LENGTH = 48.0` px，`MAX_SURFACES = 2048`；
  - 几何变动时递增 `surface_revision` 并发出 `surface_world_updated` 信号；提供 `get_walkable_surfaces`、`get_walls` 等空间查询 API；
  - 接入 `F9` 快捷键（及 Surface Pro 兼容键 `=` / `B`）实现独立的绿色平台与黄色墙体线框渲染，不捕获鼠标穿透；
  - 未创建物理碰撞体，当前小猫物理系统与 ground_y 完整保持原样，通过单元测试与全链路自验证。
- 完成 T13 多表面动态物理系统 (Multi-Surface Cat Physics)：
  - 移除单一 ground_y 假设，定义脚底接触点 `get_foot_position()`，将 `screen:ground` 作为特殊 walkable PLATFORM 接入统一多表面物理管线；
  - 在 `scripts/cat.gd` 实现 Swept Landing Detection（跨帧线段扫描判定着陆，优先停靠最小 Y 平台，杜绝穿透）；
  - 实现 One-Way Platform 特性：JUMP 向上运动穿透，下落阶段方执行着陆判定；
  - 实现每帧支撑范围检查（`support_margin = 4.0` px）与边缘脱落自动切换 FALL；表面丢失时触发脚底附近 Rebind 机制；
  - 在 `surface_world_model.gd` 追踪窗口位移增量，小猫在 `max_surface_attach_delta = 150` px 内动态跟随平移，超限或窗口关闭时安全脱落；
  - 适配 Throw / Drag 释放吸附（`snap_tolerance = 8.0` px），点击微跳与物理接触点完全解耦；
  - 支持 `F10`（及 `N` / `M`）物理调试高亮，断线时保留最后已知有效快照；
  - 新增测试用例并通过 17 项单元测试及端到端主场景集成验证，更新单文件独立版 EXE。
- 完成 T14 Windows UI Automation 元素感知系统 (UI Automation Element Perception)：
  - 使用纯 Python 标准库 `ctypes` COM 驱动 Windows 原生 `IUIAutomation` 与 `IUIAutomationTreeWalker`，实现零外部依赖的轻量感知 Provider；
  - 实施严格三维预算保护（`SCAN_BUDGET_MS = 35ms`, `MAX_DEPTH = 10`, `MAX_UI_ELEMENTS = 1000`），仅遍历前台与当前屏幕顶层窗口；
  - 严格落实隐私安全原则，不提取 Value/Text，基于稳定 `RuntimeId` 生成脱敏元素 ID；
  - 升级 `scripts/external_bridge.gd` 支持多客户端并发连接（最多 8 个），解析 `ui_snapshot` 并扩展 `TOGGLE_DEBUG_UI` 指令；
  - 建立 Godot `scripts/world/ui_element_world_model.gd` 几何容器，支持按控件类型着色渲染及空间/类型查询 API；
  - 支持 `F11`（及 Surface Pro 兼容键 `K` / `O` / `U`）调试线框切换，不破坏全屏鼠标穿透与 T13 多表面物理；
  - 新增测试并通过 22 项全量单元测试与主场景集成自验证，重新打包更新独立版 EXE。
- 完成 T16 统一表面融合与几何简化系统 (Unified Surface Fusion & Simplification)：
  - 创建统一候选标准 `scripts/world/surface_candidate.gd` 与融合器 `scripts/world/surface_fusion_builder.gd`；
  - 实现三路感知（Window + UIA + Visual）统一转化流水线，基于 50ms Debounce 合并高频更新；
  - 确立优先级矩阵（Screen 100 > Window 90 > UIA 80 > Visual 60），保留 Screen 基础边界与 Window 顶边遮挡切分；
  - 过滤 Pane/Group/Document 等非实体容器，实现同窗口同行文本（`y_diff <= 4.0px`, `gap <= 16.0px`）自动合并为连续平台；
  - 实现基于高 Z 窗口的 UI 与视觉几何动态遮挡裁剪，实现 80% 重叠去重并自动扩展几何并集；
  - 全流程坐标量化至 4px 网格，维护 600ms（Window 100ms）丢失 Grace 时序缓冲，原子提交并防抖避免物理颠簸；
  - 新增 `F13`（免 Fn 键 `H` / `Y`）融合诊断 HUD，F9 物理线框呈现全部融合表面；
  - 小猫无缝继承 T13 物理能力，无需任何特例代码即可在按钮、文本、视觉线条上 Swept Landing 落地与行走；
  - 新增测试并通过全部 34 项单元测试、端到端集成测试与多表面真实落地物理测试，重新打包独立版 PCK。
- 完成 T17 平台导航图系统 (Platform Navigation Graph)：
  - 建立 `scripts/navigation/cat_movement_capabilities.gd`，以 Cat 真实物理为唯一事实来源，推导理论最大跳高（90.0px）、飞行时间（0.857s）、Walk/Run 水平射程与二次运动求解方程；
  - 建立 `scripts/navigation/navigation_node.gd` 与 `navigation_edge.gd`，扣除小猫半宽与安全裕量，区分 `JUMP_WALK`、`JUMP_RUN`、`DROP` 动作，记录起落区间、飞行时间与综合代价；
  - 构建轻量解耦的 `scripts/navigation/platform_navigation_graph.gd`，支持 150ms Debounce 防抖与原子提交；
  - 采用空间初筛排除超出极限对；严格遵循 One-Way Platform 语义选用下落着陆解；引入连续段扫掠检测排除中间更高平台拦截的轨迹；
  - 实现 Drop 边判定与正下方首个承接平台截断；提供丰富的图查询接口（`get_node`, `get_edges_from`, `get_reachable_surfaces`, `find_nearest_node` 等）；
  - 支持 `F14`（及 Surface Pro 兼容键 `G` 切换、`T` 键打印）调试视图，默认仅渲染当前表面出度边；
  - 与小猫行为层完全解耦，不控制小猫行动；新增测试并通过 42 项全量单元测试与端到端集成验证。
- 完成 T18 自主跳跃规划与执行系统 (Autonomous Jump Planner & Execution)：
  - 创建 `scripts/navigation/traversal_plan.gd` 与 `scripts/navigation/autonomous_jump_planner.gd`；
  - 建立独立状态机（IDLE, APPROACH_TAKEOFF, READY_TO_JUMP, AIRBORNE, VERIFY_LANDING, SUCCESS, FAILED），保持与 CatState 解耦；
  - 实现基于权重的目标边挑选与 Top-3 轮盘随机选择，维护最近历史表面去重衰减（0.2）与失败目标短期黑名单（10s）；
  - 实施严格的助跑跑道物理检查（RUN_JUMP 需 $\ge 35\text{ px}$ 跑道），空间不足时安全拒绝该边；
  - 起跳点在安全区间中部轻度随机（$\pm 15\%$），到达起跳容差（$12\text{ px}$）时直接发送 JUMP，平滑继承水平移动速度，拒绝先 STOP 破坏惯性；
  - 飞行过程 100% 依赖 T13 Cat Physics，无 Teleport、无 Tween、无空中作弊吸附；
  - 着陆支持几何与位置容差匹配，未命中判定为 `MISSED_TARGET` 并安全回归 AUTO；
  - 用户显式指令、鼠标拖拽（DRAG）或指针跟随拥有最高优先级，立即熔断自主计划；
  - 支持 F15 调试视图（HUD 统计、T 起跳标记、L 落地区间与 Predicted Arc 抛物线绘制，支持快捷键 `F15` / `X` 与 `P` 单次触发）；
  - 新增测试用例并通过全部 50 项全量单元测试与端到端集成验证。
- 完成 T19 平台边缘抓取与悬挂系统 (Platform Edge Grab & Hang)：
  - 创建弱引用抓边数据结构 `scripts/world/grabbed_edge.gd`，记录表面 ID、边缘侧别（LEFT/RIGHT）与端点坐标；
  - 扩展 `scripts/world/surface_world_model.gd`，支持非屏幕、长 $\ge 32\text{ px}$ 平台端点空间查询与 $16\text{ px}$ 容差下的动态边缘重绑（Edge Rebind）；
  - 在 `scripts/cat.gd` 中新增 `CatState.EDGE_HANG`，悬挂时停用重力模拟并清空当前表面；
  - 建立稳定双前爪物理参考点（$X = \pm 12\text{ px}, Y = -20\text{ px}$），下落阶段实施严格优先级：平台正常着陆优先于抓边；
  - 引入零磁吸物理限制：容差控制为 X: 14px, Y: 20px，垂直速度上限 600px/s，水平速度上限 280px/s，严格限定外侧接近；
  - 抓左端点面向右，抓右端点面向左，悬挂偏移量 $X = \pm 6\text{ px}, Y = +18\text{ px}$；
  - 支持动态表面跟随、超大位移（$> 150\text{ px}$）脱落与表面消失时的重绑保护；
  - 区分模式：AUTO 悬挂 2.0～3.5 秒后自动脱手下落，COMMAND 模式保持挂载；
  - 新增 `RELEASE_EDGE` 指令（按键 `G`，悬挂时脱手，其余状态切换导航图），鼠标拖拽（`DRAG`）最高优先级瞬间解除抓边；
  - 兼容 T18 自主跳跃规划器：滞空抓边记录 `PARTIAL_EDGE_GRAB` 并平稳退出，不强行超时判负；
  - 支持 `F16`（免 Fn 键 `Z`）调试视图，绘制端点、L/R 标记、Grab Zone 方框与抓点；
  - 编写并通过全量 60 项单元测试与多表面物理回归测试，同步重新打包 PCK。
- 完成 T20 平台边缘攀爬翻越系统 (Platform Edge Climb-Up)：
  - 在 `scripts/cat.gd` 中新增正式状态 `CatState.CLIMB_UP`，严禁失控 Tween 绕过状态机；
  - 创建攀爬目标数据结构 `scripts/world/climb_target.gd`，严格限定只能翻上抓取的同一个表面；
  - 实现两段式受控轨迹插值（Procedural Motion）：PULL_UP（垂直拉升 0.3s）与 SHIFT_IN（向内平移 0.25s），按 delta 时间累加平滑推进，严禁瞬间 Teleport 与斜切几何；
  - 增加空间可行性与净空检查（`MIN_CLIMB_UP_PLATFORM_LENGTH = 48.0`px，落脚点上方受阻时安全拒绝翻越）；
  - 实现慢速位移动态跟随、超大位移（$>150\text{px}$）安全脱落与表面消失重绑（Rebind）；
  - 区分 AUTO 停顿 0.5s 自动翻越与 COMMAND 模式按 `H` 触发，鼠标拖拽（`DRAG`）与按键 `G`（`RELEASE_EDGE`）最高优先级瞬间中断；
  - 协同升级 T18 自主规划器：目标抓边挂起等待翻越，登顶成功结算为 `SUCCESS` 并统计 `edge_grab_recovery_success`；
  - 支持 `F17` 调试视图（绘制攀爬两段轨迹、Clearance 框与当前相位）；
  - 新增测试用例 61～70 并通过全量 70 项自动化单元测试与多表面物理回归测试，完成 PCK 同步更新。
- **T21：垂直墙面附着与双向攀爬系统交付**
  - 在 `Surface` 与 `SurfaceCandidate` 中新增 `climbable` 属性，统一为长度 $\ge 48.0\text{px}$ 的非屏幕 WALL 赋予可攀爬特性；
  - 建立 `WallAttachment` 轻量数据结构，弱引用记录表面 ID、墙面范围、附着侧向（`attach_side`）及高度锚点（`anchor_y`）；
  - 在 `Cat` 中新增 `WALL_CLING`（静止附着）与 `WALL_CLIMB`（双向攀爬）状态，严格维持下落判定优先级：`Landing > Edge Grab > Wall Attach > Fall`；
  - 限制下落抓墙最大垂直速度（500px/s）与水平速度（280px/s），单向外侧物理贴靠，零吸附；
  - 实现双向匀速爬行（60px/s），爬至 Wall Top 时自动寻找连接 Platform 并平滑转入 `EDGE_HANG` 衔接登顶翻越；
  - 实现动态墙面平移跟踪（$\le 150\text{px}$）、超限脱落与重绑（Rebind）；DRAG 拖拽及 G 键释放最高优先级打断；
  - 适配按键 `U`/`J`/`G`、`F18` 调试线框、`TAB` 切屏脱落以及外部 TCP 协议；
  - 协同适配 T18 自主规划器：挂墙挂起等待，登顶结算 `wall_climb_recovery_success`；
  - 新增测试用例 71～80 并通过全量 80 项自动化单元测试与多表面物理回归测试，完成最新 PCK 打包验证。
- **T20A：自适应猫咪缩放与 CatMetrics 统一几何度量系统交付**
  - 构建统一尺寸流动链：`Display Metrics` → `Adaptive Base Scale`（基于屏幕高比 0.10，Clamp 85~180px）→ `User Scale`（0.70~1.60）→ `CatMetrics`（唯一事实来源）；
  - 新建 `CatBodyProfile`（归一化参考几何）与 `CatMetrics`（实际像素尺寸计算），彻底消除绝对像素常量硬编码；
  - 统一锚点哲学：`Cat.position` 代表小猫脚底接触点（`foot_offset = Vector2.ZERO`），物理着陆、Swept Landing、跨帧防穿透零破坏；
  - 抓边爪点、挂墙接触点、鼠标穿透多边形与 Area2D Hitbox 均由 `CatMetrics` 动态等比输出；
  - 快捷键 `[`/`]` 调节用户缩放，`\` 重置为 1.00，`F19` 开启几何度量调试线框与 HUD；
  - 缩放突变时，挂墙/翻越/悬挂安全脱落进入 `FALL`，地面重新贴合无缝，`metrics_changed` 自动触发导航图重建；
  - Surface 融合层根据 `min_platform_length` / `min_climbable_wall_length` 动态过滤；
- **T22：攀爬与跳跃融合导航路径规划系统交付 (Climb Navigation Integration)**
  - 将平台导航图扩展为 Surface Traversal Graph：NavigationNode 引入 `NodeType.WALL`，记录 Wall 物理几何与安全攀爬区间；
  - NavigationEdge 扩展 `JUMP_TO_WALL`、`DROP_TO_WALL` 与 `WALL_TO_PLATFORM` 边类型，集成 CatMetrics 动态参数；
  - 实现基于抛物线弹道采样的 JUMP_TO_WALL 边生成，实施外侧起跳与下落附着约束，验证下落速度（$\le 500\text{px/s}$）与水平速度（$\le 280\text{px/s}$）；
  - 实现基于端点贴近的 DROP_TO_WALL 边与基于空间净空和耗时预估的 WALL_TO_PLATFORM 边生成；
  - 构建轻量 Dijkstra 路径搜索算法，支持多步 TraversalRoute 规划，设定最大搜索深度与扩展次数上限防环，优先选用 Direct Jump；
  - 升级 AutonomousJumpPlanner：扩展 11 种执行相位，支持多步连续调度、挂墙命中推进、墙顶转 EDGE_HANG 停顿与登顶路线结算；
  - 引入缩放与世界版本突变安全检测，不一致时平稳熔断取消并回归 AUTO，维持用户操作最高优先级；
  - 支持 `F20` 路线调试视图与按键 `R` 手动测试触发；
  - 编写测试用例 91～105，全量 105 项自动化单元测试、多表面物理回归测试与主场景集成测试全部通过，同步打包更新 PCK 文件。

