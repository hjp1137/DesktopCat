# T24 角色美术与动画系统全面升级成果说明

## 一、概述
在 T01～T23 及 T20A 的坚实物理与导航底座之上，本任务（T24）对 DesktopCat 完成了第一次完整的角色美术、动作动画、状态过渡与视觉反馈全面升级。彻底消除了原先占位素材仅 5 套动作且动作错乱回退的缺陷，建立了一套涵盖 16 组全状态动作的高清原创美术动画资产契约，实现了“物理事实（Physics Root）与视觉表现（Visual Root）彻底分离”，配合 Foot Lock、Squash & Stretch 落地弹性缓冲与自适应软阴影，让小猫在无 Debug HUD 辅助下即可向用户清晰传达其当前的内在意图与行为状态。

## 二、资产契约与原创美术交付
- **原创美术形象**：采用 4 倍超采样矢量抗锯齿生成的原创萌系暖金白腹猫咪形象（暖金黄背毛、雪白腹部、粉嫩耳廓与灵动杏眼），零第三方版权风险；
- **16 组完整动作矩阵（74 帧，64x64 统一规格）**：
  1. `idle` (6帧, 6fps, 呼吸起伏与轻甩尾)
  2. `walk` (6帧, 8fps, 四足交替前行与重心微荡)
  3. `run` (6帧, 14fps, 灵动奔跃与身体伸展)
  4. `sit` (4帧, 3fps, 乖巧蹲坐与收拢尾巴)
  5. `sleep` (4帧, 2fps, 团身蜷睡与沉睡呼吸)
  6. `wake` (4帧, 6fps, 睡醒揉脸与伸展懒腰)
  7. `jump` (4帧, 10fps, 起跳收腹与前爪探出)
  8. `fall` (4帧, 10fps, 下落滞空展开与尾巴扬起)
  9. `land` (3帧, 12fps, 屈膝缓冲与下压受力)
  10. `dragged` (4帧, 8fps, 悬空四肢无助扑腾)
  11. `edge_grab` (3帧, 10fps, 入壁抓牢瞬间抓扣)
  12. `edge_hang` (4帧, 4fps, 挂于平台悬空荡漾)
  13. `climb_up` (6帧, 8fps, 双爪发力撑起后肢蹬壁登顶)
  14. `wall_cling` (4帧, 4fps, 附壁爪扣与尾部贴墙平衡)
  15. `wall_climb_up` (6帧, 8fps, 交互倒替前爪向上攀爬)
  16. `wall_climb_down` (6帧, 8fps, 谨慎倒退后爪向下试探)

## 三、核心架构与解法
1. **Foot Lock 地面绝对锁定契约**：地面全部动作各帧足底像素严格锁定在画布 `Y=58`，物理 Root 不动时足底零滑动穿透；
2. **Physics Root 与 Visual Root 彻底解耦**：
   - 重构场景层级：`Cat (Node2D, 物理Root) -> VisualRoot (Node2D, 视觉Root) -> AnimatedSprite2D`；
   - 动画播放、局部朝向翻转（`flip_h`）、缩放与弹性动画仅作用于 `VisualRoot`，物理运算与几何锚点（CatMetrics）继续以 `Cat.position` 为绝对事实来源；
3. **Squash & Stretch 落地弹性缓冲**：
   - 落地事件触发 `land` 动画与 `VisualRoot` 的 `(1.18, 0.82)` 挤压，在 0.15s 内平滑回弹至 `(final_scale, final_scale)`；
4. **自适应软阴影 (`CatShadow`)**：
   - 纯 CanvasItem 实时绘制半透明径向衰减软阴影，随身体腾空高度自动缩放淡出，挂墙与悬挂时自动隐退，不参与任何鼠标与物理碰撞；
5. **动态原生纹理装配 (`CatSpriteLoader`)**：
   - 采用 `Image.load_from_file` + `ImageTexture` 动态构建 SpriteFrames，无需预生成 Godot Editor `.import` 缓存，彻底解决打包与 headless 丢失纹理缺陷；
6. **调试与展示支持**：
   - `F22` 快捷键切换动画调试 HUD，实时观测状态映射、动画名、当前帧、Foot Lock 偏置与过渡计时；
   - `7` 快捷键开启全套动画顺序自动轮播展示（Showcase 模式，每 2 秒自动切换下一套）。

## 四、自验证闭环
1. **全量单元测试**：运行 `test_runner.gd` 共 135 项测试全部通过（新增 121～135 专项动画契约与映射测试，退出码 0）；
2. **物理着陆回归测试**：运行 `test_fusion_landing.gd`（窗口、UIA、视觉线条着陆物理完全一致，退出码 0）；
3. **系统集成测试**：运行 `test_integration.gd`（感知注入、F8～F22 调试绘制、导航图、自主规划完整通过，退出码 0）；
4. **编译检查**：`--check-only` 语法检查退出码 0；
5. **PCK 打包**：独立打包 `DesktopCat.pck` 与 `DesktopCat_Standalone.pck` 完整封包 74 张新美术资产。

## 五、形象残缺修复与单机感知自启动成果 (针对用户反馈)
1. **猫咪形象残缺修复与造型全盘重绘**：
   - 彻底修复 `tools/generate_cat_art.py` 中躯干坐标未乘超采样倍率 $s$ 的严重数学错误；
   - 重新设计圆润饱满的二头身萌系橘白猫形象，严密保持头身相连（身体重叠头颈部 10px），配以圆脸立耳、水灵大眼高光、白胸腹、粉肉垫与自然四肢透视，74 张全套序列帧已重新生成；
2. **桌面感知服务自动拉起与安全退出**：
   - 在 `scripts/main.gd` 中实现 Windows 环境自动检测并静默拉起 `perception_service.py`（使用 `pythonw.exe` 避免黑色控制台窗口），并在主窗口退出时通过 `OS.kill` 彻底销毁子进程；
   - 彻底修复 `run_desktop_cat.bat` 编码，采用纯 ASCII 格式，根除 Windows CMD 执行 UTF-8 中文时的解析截断乱码错误；
   - 修复 `perception_service.py` 与 `visual_perception.py` 缺失根目录 `sys.path` 导致的 `ModuleNotFoundError: No module named 'tools'` 异常；
   - 修复 `scripts/main.gd` 中 `_spawn_fallback_demo_surfaces` 的表面字典属性访问（`surfaces_by_id`）；
   - 增加纯单机内置演示平台兜底：若 3 秒内未收到任何外部实体表面，自动在屏幕中央生成桌面平台与垂直攀爬柱，确保小猫在任何无配置环境下都能随时展现跳跃与攀爬能力。

## 六、行走对齐、桌面猫爬架与生动鼠标交互交付 (针对用户四项问题)
1. **足底严格对齐，根治行走下沉截断 (问题1)**：
   - 定位视觉截断根因：`AnimatedSprite2D` 原点在画布中心 `(32, 32)`，足底位于 `Y=58`（局部偏置 `+26px`），缩放后下沉 70+ 像素陷入物理表面；
   - 在 `scenes/cat.tscn` 与 `scripts/cat/cat_animation_controller.gd` 中统一配置 `animated_sprite.offset = Vector2(0, -26)`，将足底像素精确锚定于物理 Root `Y=0`；
   - 在 `scripts/cat.gd` 中将碰撞形状（`col_shape`）动态居中于 `-metrics.hitbox_size.y * 0.5`，使小猫体态完整挺立于表面之上，四足踏地，全身体态完整呈现。
2. **桌面实体猫爬架 (Cat Gym) 绘制与表面常驻 (问题2、4)**：
   - 新建 `scripts/world/cat_gym.gd`：以日系温馨原木与粗麻绳材质在桌面直观绘制多层实体猫爬架（圆角原木底座、粗麻绳主攀爬立柱、三层跳台配米白软垫与萌系猫爪印）；
   - 导出三层跳台 PLATFORM 与两根贯穿式垂直攀爬 WALL（`climbable = true`），物理表面在世界更新中永久常驻；
   - 自主探索控制器启动后即可自动规划选择猫爬架跳台路线，实现无需外部窗口也可随时攀爬。
3. **鼠标生动交互与即时反馈升级 (问题3)**：
   - 修复点击判定区域：由硬编码 Rect2 升级为基于 `metrics.hitbox_size` 动态计算，全身与头部均能灵敏命中；
   - 新建 `scripts/cat/floating_heart.gd`：左键轻点小猫时头顶弹出上浮淡出的萌系粉嫩爱心气泡，小猫身体触发果冻弹性回弹；
   - 支持鼠标右键点击：指令小猫立刻乖巧坐下（`SIT`）；
   - 支持拖拽动态物理倾斜（Drag Tilt）：拖动时根据速度产生柔和惯性倾斜摆动，释放后平滑回正。
