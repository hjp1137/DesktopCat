# T24 角色美术与动画系统升级方案 (Art & Animation Upgrade)

## 一、方案背景与架构定位
在 T01～T23 完成物理系统、三路感知融合、导航图、自主攀爬与全域探索之后，DesktopCat 核心机制已达成 M1 阶段完整闭环。目前猫咪角色仍在使用初始阶段的 64x64 低精简笔画 Placeholder（仅包含 5 个基础循环），缺乏跳跃、下落、落地缓冲、抓边悬挂、翻越登顶、抓壁攀爬与拖拽等关键状态的专属动画。
本方案旨在构建高品质、视觉统一且富有生命力的原创 2D 卡通桌面宠物猫形象，建立完整的动画契约（Animation Contract）、物理与视觉根节点分离架构（Physics Root vs. Visual Root）、脚底锁定（Foot Lock）、独立动画控制器（`CatAnimationController`）与轻量阴影反馈系统，在不破坏任何底层物理与导航的前提下，大幅提升肉眼辨识度与沉浸感。

## 二、系统层级与核心约束
```
Cat (Node2D, Physics Root) ── [Cat.position 绝对物理事实来源: 脚底接触点]
  ├─ CatShadow (轻量半透明椭圆阴影, 随高度/悬挂缩放淡化, 纯绘制零阻挡)
  ├─ VisualRoot (Node2D, 视觉根节点, 处理落地挤压 Squash、跳跃拉伸与朝向)
  │    └─ AnimatedSprite2D (完整 16 套动画契约, 64x64/128x128 严格画布与锚点)
  ├─ Area2D (Hitbox, 基于 CatBodyProfile 动态缩放)
  └─ CatAnimationController (状态映射、过渡平滑、动画计时与调试模式)
```
- **核心红线**：严禁动画驱动真实物理位移；严禁在动画帧中修改 `position`、`velocity` 或破坏 `CatMetrics`；
- **足底锁定 (Foot Lock)**：地面状态（IDLE, WALK, RUN, SIT, SLEEP, LAND）各帧接触点 Y 严格对齐，禁止上下跳变与脚滑；
- **视觉一致性**：所有 16 组动画均严格使用同一定义的原创橘白小猫（暖色调、粉耳内轮廓、圆润脸型、白色爪套与灵动尾巴）；
- **全状态辨识**：即使关闭所有调试线框，仅凭肉眼也能清晰区分 16 种动作形态。

## 三、完整动画契约 (Animation Contract)
| 动画名称 | 帧数 | 目标帧率 (FPS) | 循环模式 | 动作特征与关键帧要求 |
|:---|:---|:---|:---|:---|
| `idle` | 6 帧 | 8 FPS | Loop | 自然四足站立、轻微呼吸腹部起伏、尾巴优雅摇摆、微眨眼 |
| `walk` | 6 帧 | 10 FPS | Loop | 节奏四足交替漫步、头部与背部轻微起伏、尾巴平衡摆动 |
| `run` | 6 帧 | 14 FPS | Loop | 身体压低流线型大步冲刺、四足离地爆发力、尾巴挺直微扬 |
| `sit` | 4 帧 | 4 FPS | Loop | 臀部坐地收腿、前爪直立端坐、尾巴贴地环绕身体 |
| `sleep` | 4 帧 | 3 FPS | Loop | 身体蜷缩成团、闭目沉睡、腹部缓慢呼吸起伏 |
| `wake` | 4 帧 | 6 FPS | Once | 睁眼抬头、伸展前爪弓背打哈欠、舒展站起过渡至 idle |
| `jump` | 4 帧 | 10 FPS | Loop | 起跳蹬地蓄力、身体向上舒展、后足收拢向上跃起 |
| `fall` | 4 帧 | 10 FPS | Loop | 身体前倾下倾、四爪轻微张开准备缓冲、尾巴上翘迎风 |
| `land` | 3 帧 | 12 FPS | Once | 落地瞬间四爪触地深屈、VisualRoot 纵向 Squash 挤压缓冲后回弹 |
| `edge_grab` | 3 帧 | 12 FPS | Once | 空中伸爪抓取平台边缘转入挂壁过渡瞬间 |
| `edge_hang` | 4 帧 | 6 FPS | Loop | 双前爪牢扣边缘、后半身悬空垂直下坠、身体微幅柔和晃动 |
| `climb_up` | 6 帧 | 10 FPS | Once | 前爪撑起上拉（PULL_UP）-> 后足蹬墙翻上（SHIFT_IN）-> 稳立 |
| `wall_cling` | 4 帧 | 6 FPS | Loop | 面向垂直墙面、四爪抓附墙体、背部拱起观察 |
| `wall_climb_up` | 6 帧 | 10 FPS | Loop | 左右前爪与后爪交替向上攀抓攀爬、身体随爬行微幅摆动 |
| `wall_climb_down`| 6 帧 | 10 FPS | Loop | 后向倒退交替探爪抓壁、平稳匀速缓降 |
| `dragged` | 4 帧 | 8 FPS | Loop | 被鼠标提起悬空、四爪自然下垂无助轻晃、尾巴垂坠摇晃 |

## 四、CatAnimationController 与节点解耦
1. **VisualRoot 节点建立**：
   - 在 `Cat` 根节点下建立 `VisualRoot (Node2D)`，所有缩放（Scale）与瞬时弹性（Squash & Stretch）作用于 VisualRoot；
   - `VisualRoot.position = foot_offset`，确立足底为 `Vector2.ZERO`，实现绝对 Foot Lock；
2. **CatAnimationController 状态机**：
   - 监听 `Cat.state_changed` 与物理事件（落地 `landed`、起跳、抓壁、攀爬相位变化）；
   - 处理优先级仲裁：`DRAGGED > CLIMB_UP > WALL_CLIMB > WALL_CLING > EDGE_HANG > JUMP/FALL > LAND > WAKE > SLEEP > SIT > RUN > WALK > IDLE`；
   - 落地时触发非破坏性 Visual Squash（$Y=0.82, X=1.18$），经 0.15 秒平滑插值回 $1.0$；
3. **轻量阴影 (CatShadow)**：
   - 纯 `_draw` 绘制轻量半透明软椭圆，随高度提升动态衰减透明度与半径，无任何碰撞区域，完全穿透鼠标。

## 五、美术素材生成与目录规划
1. **统一画布与锚点规范**：
   - 采用标准高精 `64x64`（或等比矢量抗锯齿）统一画布，足底接触点严格定于 `(32, 58)`；
   - 挂边前爪锚点定于 `(46, 38)`，挂墙贴靠边定于 `(48, 38)`；
   - 保持所有动作角色身型一致：暖橙色身躯、乳白腹部与爪尖、粉色内耳、黑棕灵动眼眸与虎斑尾纹。
2. **资产目录结构**：
   ```
   assets/cat/
     ├─ sprites/
     │    ├─ idle_*.png (6帧)
     │    ├─ walk_*.png (6帧)
     │    ├─ run_*.png (6帧)
     │    ├─ sit_*.png (4帧)
     │    ├─ sleep_*.png (4帧)
     │    ├─ wake_*.png (4帧)
     │    ├─ jump_*.png (4帧)
     │    ├─ fall_*.png (4帧)
     │    ├─ land_*.png (3帧)
     │    ├─ edge_grab_*.png (3帧)
     │    ├─ edge_hang_*.png (4帧)
     │    ├─ climb_up_*.png (6帧)
     │    ├─ wall_cling_*.png (4帧)
     │    ├─ wall_climb_up_*.png (6帧)
     │    ├─ wall_climb_down_*.png (6帧)
     │    └─ dragged_*.png (4帧)
     └─ README.md (版权与帧率规范说明)
   ```

## 六、自验证闭环规划
1. **单元测试与状态映射覆盖**：
   - 在 `scripts/test_runner.gd` 中新增测试 121 至 135，覆盖 16 套动画映射、缺失动画兜底、VisualRoot 挤压恢复、Foot Lock 接触点对齐、F22 调试视图与顺序预览模式；
2. **回归与真实主场景验证**：
   - 运行多表面物理回归 `test_fusion_landing.gd` 与主场景集成 `test_integration.gd`；
3. **资源打包与交付**：
   - 重新编译生成 `build/DesktopCat.pck` 与 `DesktopCat_Standalone.pck`。

## 七、形象残缺修复与美术质感全盘重绘 (针对用户反馈)
1. **根本原因定位**：
   - `tools/generate_cat_art.py` 中 `draw_cat_body_grounded` 的 `bx`、`by` 传入的是 1x 逻辑像素，但在超采样绘制时未乘 `s`（倍率4），导致躯干椭圆被画在左上角 `(30, 41)`，与头部 `(168, 116)` 彻底断裂，身体大面积丢失，视觉上仅剩头部与悬空爪子；
2. **高品质猫咪矢量解剖结构重构**：
   - 统一使用 `S(x) = x * s` 统一坐标换算，彻底杜绝坐标漏乘；
   - 重新设计圆润饱满的二头身卡通猫：
     - **头部**：加大幅度增加萌系大脸、圆润粉白耳廓、水灵杏眼与反光、立体粉色小三瓣嘴与胡须；
     - **躯干**：丰满圆滚的暖橙躯干紧贴头部后下方，大片雪白胸腹毛与虎斑背纹；
     - **四肢与肉垫**：四只软萌白爪套小短腿，站立四足稳踩 `Y=58`，行走与奔跑时前后生动交替；
     - **尾巴**：毛茸茸向上轻弯的厚实猫尾，随动作摇曳；
   - 重写全部 16 组共 74 张序列帧，全面提升辨识度与可爱度。

## 八、单机感知自启动与攀爬激活 (针对无感知不攀爬问题)
1. **问题成因**：
   - 用户直接双击运行 `DesktopCat_Standalone.exe` 时，外部 Python 感知服务（`perception_service.py`）并未启动，小猫世界内仅有屏幕底部的 `screen:ground`，无任何窗口与垂直墙面，导致小猫只能在地面平移，无法执行跳跃与攀爬；
2. **自动拉起机制**：
   - 在 `main.gd` 中检测如果运行在 Windows 平台且本地存在 `tools/perception/perception_service.py`，启动后自动异步唤起感知子进程（优先尝试 `pythonw.exe` 避免弹出黑色控制台，回退尝试 `python.exe`）；
   - 主场景退出时通过 `OS.kill(perception_pid)` 自动清理，实现即开即用零配置；
   - 在没有感知服务的纯单机环境下，提供兜底内置平台，确保无论如何小猫都能展现攀爬跳跃能力。

## 九、足底接触点严格对齐与下沉修复 (解决行走显示不全)
1. **根本原因**：
   - 64x64 画布中足底位于 $Y=58$。因 `AnimatedSprite2D` 默认 `centered = true`，中心 $(32, 32)$ 落在原点，导致实际足底落在 $Y=+26$ 像素；
   - 在 2.8x 屏幕缩放比例下，足底相对物理表面下沉了 $26 \times 2.8 \approx 73\text{px}$，使小猫下半身、肚皮与四肢完全沉入表面下方（如文字行下），视觉上仅露猫头与背脊；
2. **解法**：
   - 将 `AnimatedSprite2D.offset.y`（或 `position.y`）设置为 `-26.0` 像素，使图片中足底 $Y=58$ 严格对齐到 `VisualRoot` 的 $(0, 0)$，即小猫物理足底接触点 `Cat.position`；
   - 小猫的全部身躯、四足、肉垫与肚皮均稳稳立于物理表面之上，彻底解决行走显示不全缺陷。

## 十、屏幕内容虚拟猫爬架可视化与跳跃攀爬打通方案 (修正实体猫爬架误解并打通文本行跳跃攀爬)
1. **剥离实体木架，回归纯正“屏幕内容虚拟化”**：
   - 移除写死的木质猫爬架（`cat_gym.gd`），还原屏幕真实内容作为唯一游乐场的设计原则；
2. **虚拟猫爬架可视化层 (Virtual Cat Gym Overlay) 默认展示**：
   - 将系统从屏幕（文本行、按钮、窗口边缘等）自动感知融合出的所有表面以富有现代感与辨识度的高亮半透明阶梯与立柱呈现：
     - 文本行与窗口顶边（PLATFORM）：柔和发光薄荷绿台阶，带胶囊端点与微光顶沿；
     - 窗口侧边与立柱（WALL，`climbable=true`）：半透明暖橙金垂直抓柱，带爪攀刻度纹理；
     - 跳跃与下落可达弧线：展示从小猫当前台阶可跳跃/下落的目标路径；
   - 默认直接开启展示，用户肉眼一眼便知屏幕上哪些字和窗口被虚拟化成了跳台；支持 `V` / `F9` 随时一键切换；
3. **修复文本行与窗口立柱间的跳跃与下落边生成**：
   - 修复 `_check_drop_edge` 在端点重叠时的边界容差收缩缺陷（放宽 `landing_margin` 限制），打通重叠文本行之间的走落（DROP）；
   - 优化 `_check_jump_edge` 在近距离密集文本行间的弹道遮挡检测，允许小猫逐级向上跳跃（JUMP）；
4. **修复密集文本行的翻越净空误阻 (Clearance Check)**：
   - 修复 `can_climb_up_surface` 将上方同向 `TOP` 文本跳台误当作天花板导致死循环 `CLEARANCE_BLOCKED` 的严重 Bug；
5. **激活自主探索在屏幕台阶与立柱间的攀爬与跳跃调度**：
   - 提升小猫在文本行上的探索决策活跃度，激励小猫向上爬高、跳跃与攀爬窗口立柱，杜绝单纯横向徘徊。

## 十一、鼠标多维生动交互反馈 (已交付)
1. 鼠标左键轻点小猫飘浮粉红爱心气泡并触发果冻弹性回弹；
2. 鼠标右键点击直接指令坐下（`SIT`）；
3. 鼠标拖拽带速度惯性物理倾斜，松手平滑回正。

## 十二、验证方案
1. 运行场景诊断脚本，验证密集文本行之间成功生成 JUMP 与 DROP 导航边；
2. 验证小猫在密集文本行挂边翻越时净空判定通过，无 `CLEARANCE_BLOCKED` 报错；
3. 验证桌面默认呈现美观的半透明“虚拟猫爬架”台阶与立柱展示；
4. 运行全量 135 项单元测试与集成测试，验证零回归；
5. 重新打包独立程序 PCK 并完成自验证。
