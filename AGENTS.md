# 全局系统指令（AGENTS.md）

本文件作为 AI 编码助手的全局规则。目标是让 AI 助手少空谈、多交付；少猜测、多验证；少口头承诺、多物理留痕。

---

## 1. 执行策略与人机协作机制

- **三级人机协作确认机制（严禁未经沟通直接盲改）**：
  1. **简单问题 / 明确 Bug**：先向用户清晰解释出问题的根因，简要说明拟定的修改要点，然后直接动手实施修改与自验证闭环；
  2. **详尽的任务指令（无任何疑惑）**：先简要说明对任务的核心理解与执行要点，然后直接动手实施修改与自验证闭环；
  3. **复杂问题 / 涉及方案取舍 / 需求存在理解歧义**：严禁私自脑补或擅自改动代码。必须先停下来向用户说明现状、分析原因并列出可选方案，等待用户明确确认后再行动手修改。
- **先理解现有工程，再改代码**：改动前必须检索相关文件、已有实现、已有公共方法和历史任务记录，避免凭空重构。
- **Offline-First 本地验证**：优先在本地服务、测试脚本、接口请求、构建命令和浏览器自动化中闭环验证，不依赖高成本线上 API 盲目试错。
- **简洁至上**：优先使用简单、可维护、与现有代码一致的实现，避免过度工程化。
- **高内聚低耦合**：新增功能前必须查找可复用逻辑。共性能力应抽象复用，避免复制粘贴式扩散。
- **严禁夸大**：不要使用“完美运行”“彻底解决”“全链路完整验证”“精准控制”等未经完整验证的表述。
- **任务边界清晰**：只完成用户当前任务，不擅自进入下一阶段，不擅自实现高风险延伸功能。

---

## 2. 运行期行为规范

- 使用中文与用户交互。
- 调用工具时，`toolAction`、`toolSummary`、`Description` 等说明性字段尽量使用中文，保证 IDE 交互提示可读。
- 需要获取网页内容时，优先使用 `curl`。
- 回答“最近”“最新”“当前”等时效性问题时，必须先确认当前时间或搜索最新资料。
- 不要频繁使用 `powershell.exe -Command` 包裹命令，优先直接执行 `python`、`pytest`、`pnpm` 等命令。
- 使用编辑工具时避免一次性输出过大的文件块。原则上单次新增或编辑控制在约 100 行以内；确因规则文件、配置文件或测试文件需要超过时，应分段写入或说明原因。
- 对一次性查找、搜索、构建、测试命令，执行完毕且获取结果后，应及时结束对应后台任务，避免闲置占用资源。
- 对为 E2E 临时启动的前端、后端服务，不得在测试尚未完成时提前关闭；测试结束后应统一关闭或说明残留原因。

---

## 3. AI 编程完成规则：实现后必须自验证

AI 编程助手的目标不是“写完代码”，而是“交付经过验证、可以运行的结果”。

### 3.1 完成定义

任何编码任务，只有同时满足以下条件，才可以声明“已完成”：

1. 代码已经按需求实现；
2. 已经自行运行必要的验证命令；
3. 验证结果通过，或无法验证的原因已如实说明；
4. 没有遗留明显错误、失败测试或未处理异常；
5. 最终回复明确列出实际执行过的验证命令和结果。

禁止仅凭代码修改、静态推测或主观判断宣布完成。

### 3.2 验证优先级

完成代码修改后，必须根据项目情况自动选择并运行验证命令。优先从以下位置识别可用命令：

- `package.json`
- `pnpm-lock.yaml` / `yarn.lock` / `package-lock.json`
- `Makefile`
- `pyproject.toml`
- `requirements.txt`
- `Cargo.toml`
- `go.mod`
- `pom.xml`
- `build.gradle`
- `README.md`
- `.github/workflows`

常见验证包括但不限于：依赖安装检查、类型检查、lint、单元测试、集成测试、构建、数据库 schema 校验、API 启动、接口请求验证、页面功能本地运行验证。

如果项目提供明确验证命令，应优先使用项目已有命令，不要自行臆造命令。

### 3.3 必须执行验证闭环

每次完成修改后，必须执行以下闭环：

1. 运行相关验证命令；
2. 如果失败，阅读错误信息；
3. 修复问题；
4. 再次运行验证；
5. 重复直到通过，或明确说明无法继续的原因。

不得在验证失败时声称任务完成。

### 3.4 无法验证时的处理

如果因为环境限制、依赖缺失、权限不足、数据库不可用、外部服务不可访问等原因导致无法完成验证，必须如实说明：

- 已完成的代码修改；
- 尝试运行过的命令；
- 失败原因或阻塞点；
- 哪些功能尚未被验证；
- 需要用户提供什么条件才能继续验证。

这种情况下只能说“代码已修改，但验证未完成”，不能说“功能已完成”。

### 3.5 禁止行为

禁止以下行为：

- 未运行测试却声称测试通过；
- 未运行构建却声称构建通过；
- 把“应该可以”“理论上可行”当作完成结论；
- 让用户代替自己完成基础验证；
- 隐藏失败命令或错误信息；
- 只修改代码但不运行任何检查；
- 跳过项目已有测试、lint、typecheck 或 build 命令；
- 因任务看似简单就省略验证。

---

## 4. 前端页面与 E2E 自动化验收规则

凡涉及前端页面、路由、菜单、列表、Tab、弹窗、抽屉、按钮、表单、筛选、状态标签、空状态、确认框、批量操作、页面跳转、顶部页签等 UI 或交互改动的任务，必须优先使用 Playwright 或项目已有 E2E 工具进行自动化验证，并生成截图留证。

后端纯接口、脚本、数据库迁移等不涉及页面展示的任务，可不强制截图，但仍必须完成接口、测试、构建或等价验证。

### 4.1 Playwright / E2E 优先

- 项目已存在 Playwright、Cypress、Vitest Browser、Browser Tools 或等价浏览器自动化能力时，必须优先复用。
- 优先在项目现有 e2e 目录和测试文件中新增或扩展测试，不另起孤立脚本。
- 涉及前端页面的任务，应尽量新增与任务号对应的 E2E 用例。
- E2E 测试应覆盖用户真实操作路径，不只断言组件存在。
- 能自动点击、输入、切换、打开弹窗、提交确认、校验结果的，必须尽量自动化完成。
- 不得用“人工已看过页面”替代可自动化完成的 E2E 验证。
- 因真实环境、数据、权限或浏览器自动化限制导致无法执行 E2E 时，必须如实记录失败原因、替代验证方式和未覆盖风险。

### 4.2 E2E 应覆盖的内容

根据本次任务实际改动，尽量覆盖：

1. 页面能正常打开；
2. 页面标题、说明文案、关键区域存在；
3. 菜单入口和路由正确；
4. Tab 切换正常，且不新增多余顶部页签；
5. 表格、资源树、卡片、列表有数据状态正常；
6. 空数据时显示清晰空状态；
7. 搜索、筛选、分页、刷新等基础交互正常；
8. 新增按钮可见、可点击、禁用状态正确；
9. 弹窗、抽屉、确认框能正常打开和关闭；
10. 表单校验有效，必填项缺失时不能提交；
11. 批量操作前有二次确认和影响范围提示；
12. 成功、失败、阻断、不可操作状态有明确提示；
13. 页面操作后数据刷新正确；
14. 顶部 Vben 页签不会因内部 Tab、query 或抽屉操作重复新增；
15. 高风险操作不会误触发下游动作，例如自动签发、写入 Outbox、调用向量库、调用大模型，除非任务明确要求。

### 4.3 自动截图要求

涉及前端页面改动的任务，必须使用 Playwright 或等价浏览器自动化方式，对本次改动涉及的关键页面状态截图。

截图属于成果说明的证据附件，最终审核截图统一保存到：

```text
Walkthroughes/screenshots/YYYYMMDD_任务号_任务名/
```

示例：

```text
Walkthroughes/screenshots/20260630_任务20E11_正式签发与Outbox写入最小闭环/
```

Playwright 默认生成的 `test-results/` 可作为临时测试产物，但最终用于用户审核和归档的截图，必须复制或直接保存到 `Walkthroughes/screenshots/任务目录/`。

截图文件名应使用序号 + 页面名 + 功能点 + 状态，例如：

```text
01_语料资产_待签发资源_签发前.png
02_语料资产_签发预检抽屉_eligible_true.png
03_语料资产_正式签发确认框.png
04_语料资产_签发成功提示.png
05_语料资产_已签发资产_签发后.png
06_语料资产_同步队列_待同步.png
```

不得把所有截图散放到 `Walkthroughes/` 根目录，也不得只保留在较深的前端测试临时目录中。

### 4.4 截图覆盖范围

每个前端页面任务至少截图与本任务相关的部分：

1. 页面初始状态；
2. 本次新增或修改的主页面；
3. 本次新增或修改的 Tab；
4. 本次新增或修改的列表、表格、资源树或卡片；
5. 本次新增或修改的抽屉、弹窗、确认框；
6. 本次新增或修改的按钮区域；
7. 空状态；
8. 有数据状态；
9. 阻断、失败、禁用、不可操作状态；
10. 批量操作确认框；
11. 表单校验失败状态；
12. 操作成功后的刷新状态；
13. 顶部 Vben 页签状态，尤其是验证是否新增重复页签。

### 4.5 成果说明中的截图清单

凡涉及前端页面的任务，`Walkthroughes/` 成果说明文件中必须新增：

```markdown
## 自动截图清单
```

逐项列出 `Walkthroughes/screenshots/任务目录/` 下的截图真实路径，并说明每张截图对应的验证点。截图路径必须是仓库内实际生成路径，不得只写计划路径；成果说明中的截图清单必须与实际文件一致。

### 4.6 无法截图或无法 E2E 时的处理

如果因为真实数据不足、页面入口缺失、服务无法启动、浏览器自动化失败、权限限制、依赖缺失、外部服务不可用等原因，导致某些状态无法自动截图或无法完成 E2E，必须在成果说明中如实说明：

1. 哪些 E2E 测试未能执行；
2. 哪些截图未能生成；
3. 失败命令或阻塞原因；
4. 已执行的替代验证方式；
5. 哪些功能仍未被自动化验证；
6. 需要用户人工补充检查的页面或状态。

不得伪造截图，不得伪造 E2E 通过结果，不得把未截图、未自动化验证的状态写成已经通过 GUI 验证。

### 4.7 E2E 与基础工程验证的关系

Playwright / E2E 不能替代基础工程验证。涉及前端项目时，仍必须尽量执行：

```bash
pnpm -F <frontend-package> run typecheck
pnpm -F <frontend-package> run build
```

如项目提供 lint、unit test、e2e test，应尽量执行：

```bash
pnpm -F <frontend-package> run lint
pnpm -F <frontend-package> run test
pnpm -F <frontend-package> run e2e
```

如果 lint 因已知环境问题失败或无法运行，必须如实记录，不能写成通过。

---

## 5. 数据库写操作与高风险操作规则

凡涉及数据库写入、批量状态变更、资产签发、Outbox 写入、迁移脚本、批处理任务等高风险操作，必须遵守以下规则：

1. 操作前记录关键表计数或关键记录状态；
2. 操作后再次记录关键表计数或关键记录状态；
3. 在成果说明中列出前后对比；
4. 明确说明是否创建资产、是否写入 Outbox、是否修改审核状态、是否触发下游任务；
5. 禁止清库、删表、重建库或大规模不可逆写入，除非用户明确授权；
6. 不得为了测试而破坏已有数据；
7. 若数据已经被前序任务处理过，应验证幂等或已处理状态。

对于 nfmCuration 类任务，尤其要记录：

- `CuratedCorpusAsset` 数量变化；
- `VectorSyncOutbox` 数量变化；
- 目标资源下待签发 / 已签发数量；
- 审核状态是否被错误修改；
- 是否消费 Outbox；
- 是否调用向量库或大模型。

---

## 6. 物理存储与工程流规则

### 6.1 计划文件与成果说明

开始编写或修改业务代码前，必须先将实现计划写入根目录 `plans/` 目录。

开发并本地测试完成后，必须将成果说明写入根目录 `Walkthroughes/` 目录。

文件名必须使用正常中文，不得使用 URL 编码或转义中文。项目如有任务编号，文件名必须包含任务编号，例如：

```text
20260630_任务20E11_nfmCuration_正式签发与Outbox写入最小闭环计划.md
20260630_任务20E11_nfmCuration_正式签发与Outbox写入最小闭环成果说明.md
```

### 6.2 同任务更新与新任务区分

- 对同一任务内的补充修改、返工修正、同一次验收中的小范围补丁，优先更新同一份 `plans/` 和 `Walkthroughes/` 文件。
- 对用户明确下发的新任务号、新阶段任务或独立验收任务，必须新建对应任务号的 `plans/` 和 `Walkthroughes/` 文件。
- 不得把不同任务号的成果混写进同一份成果说明，除非用户明确要求合并。

### 6.3 历史上下文获取

接收新任务、进行 AI 编程或故障诊断时，必须主动扫描并阅读根目录下 `plans/` 和 `Walkthroughes/` 的相关历史方案及成果说明，继承既定设计和架构变迁，严禁凭空重构。

### 6.4 开发日记

凡是有代码变更、修补或开发工作的日期，任务结束前必须在项目根目录 `开发日记.md` 中追加或合并当天开发成果总结。

同一天内的补充修改应合并到当天条目中，不要产生多条零散记录。

文风必须简洁平实、严谨客观，只记录工程事实和核心解法。

---

## 7. 最终回复格式

每次编码任务结束时，最终回复必须包含以下内容。

### 实现内容

简要说明完成了什么功能或修复了什么问题。

### 修改文件

列出主要修改文件及作用。

### 自验证结果

列出实际运行过的命令及结果：

- 通过；
- 失败后已修复并重新通过；
- 未能运行，并说明原因。

### E2E 验证结果

如涉及前端页面，列出 Playwright 或等价 E2E 验证命令与结果。

如不涉及前端页面，写：不涉及。

### 自动截图清单

如涉及前端页面，列出 `Walkthroughes/screenshots/任务目录/` 下的截图路径及对应验证点。

如不涉及前端页面，写：不涉及。

如应截图但未能截图，必须说明原因。

### 未验证项

如果仍有未验证内容，必须明确列出。若全部适用验证已通过，写：无。

### 结论

- 关键验证全部通过时：`任务已完成，并已通过自验证。`
- 仍存在未验证项时：`代码已修改，但仍存在未验证项，不能确认完全完成。`

---

## 8. 任务终结信号

在确认所有适用流程均已完成后，包括代码编写、本地调试、验证、前端任务的 E2E 自动化与自动截图、`plans/` 写入、`Walkthroughes/` 写入、开发日记追加，AI 助手最终回复的最开头必须输出固定暗号：

```text
喵喵。
```

如果仍存在未验证项、截图缺失、计划或成果说明未写入，不得输出该完成暗号。

注：若当前轮次属于“复杂问题/方案确认阶段”（即先停下来向用户确认情况与方案时），此时尚未进行实质代码交付，应正常输出分析与可选方案与用户讨论，无需输出五段式验收报告和完成暗号。
